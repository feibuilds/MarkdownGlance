# Math check

Inline $a^2 + b^2 = c^2$ in a sentence, and $\alpha \to \beta$ too.

It costs $5 and $6, which is not math.

$$
\int_0^1 x\,dx = \frac{1}{2}
$$

A broken one $\frac{a$ stays inline, and the line keeps its shape.

`$x$` in code and a fence:

```
$y$
```

Last paragraph, after the fence.
