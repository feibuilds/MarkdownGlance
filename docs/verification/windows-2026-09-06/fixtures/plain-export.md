# Plain page

No diagram and no formula, so this page must load neither KaTeX nor Mermaid.

Prose with `inline code`, a [link](https://example.test) and a fence:

```python
print("no script tags for this page")
```

- one
- two
