A document with no headings at all.

Two paragraphs, a list and a fence, and not one `#` at the start of a line.

- one
- two

```python
print("no headings here either")
```

The outline over this file must be empty.
